# 9. Write a python script to check if a string contains only characters of the alphabet.

print("String contains only alphabets" if(input("Enter a string: ").isalpha()==1)    else"String does not contain only alphabets")